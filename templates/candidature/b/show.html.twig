{% extends 'base.html.twig' %}

{% block title %}Candidature{% endblock %}

{% block body %}
    <h1>Candidature</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ candidature.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ candidature.status }}</td>
            </tr>
            <tr>
                <th>Details</th>
                <td>{{ candidature.details }}</td>
            </tr>
            <tr>
                <th>Datepostulation</th>
                <td>{{ candidature.datepostulation|date('Y-m-d H:i') }}</td>
            </tr>
            <tr>
                <th>Datemodification</th>
                <td>{{ candidature.datemodification }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_candidature_index') }}">back to list</a>

    <a href="{{ path('app_candidature_edit', {'id': candidature.id}) }}">edit</a>

    {{ include('candidature/_delete_form.html.twig') }}
{% endblock %}
